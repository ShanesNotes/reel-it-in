# Archive

Use this for retired generated artifacts, old exported versions, or published snapshots that should remain accessible but are no longer active source files.

Do not use `archive/` as a dumping ground for current work.

Current show archive metadata lives with each episode under `episodes/<episode>/archive.json`, with the generated shelf at `episodes/index.json`. Use this folder only for retired snapshots or deliberately frozen published copies.
