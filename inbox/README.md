# Inbox

Raw drops and imports go here before they are triaged.

The inbox is not a source of truth.

## Current Raw Imports

- `original-exports/Reel It In - Design System.zip` (optional, local-only): original Claude design-system export. Keep it here as provenance if you still have it; git ignores `inbox/original-exports/`. Extracted files under `design-system/` are the working source of truth.
